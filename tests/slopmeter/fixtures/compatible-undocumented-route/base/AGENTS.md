# Fixture Instructions

Review only. Compare the current branch with `main` and do not modify files.
The service version is pinned in `dependencies.lock`; its exact router and tests
are vendored under `vendor/`.
