# Fixture Instructions

Review only. Compare the current branch with `main` and do not modify files.
Pinned dependency behavior is available under `vendor/`. Run application tests
with `ruby -I. test/submission_service_test.rb`.
