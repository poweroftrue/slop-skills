# Fixture Instructions

Review only. Compare the current branch with `main` and do not modify files.
The documented application runner is `ruby bin/app-run`; application tests run
with `ruby -I. test/order_presenter_test.rb`.
