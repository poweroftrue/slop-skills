# Fixture Instructions

Review only. Compare the current branch with `main` and do not modify files.
Read `docs/session-store-contract.md` before deriving input variants or judging
the scan cost.
Run tests with `ruby -I. test/session_counter_test.rb`.
