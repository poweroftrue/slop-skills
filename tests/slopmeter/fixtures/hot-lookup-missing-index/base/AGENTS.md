# Fixture Instructions

Review only. Compare the current branch with `main` and do not modify files.
Run functional coverage with `ruby -I. test/order_lookup_test.rb`. The documented
application-loaded query-plan probe is `ruby bin/profile-order-lookup`; its
representative dataset and latency target are documented under `docs/`.
