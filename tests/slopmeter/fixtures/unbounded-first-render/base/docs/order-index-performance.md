# Order index performance contract

The largest current merchant has 50,000 orders. The synchronous initial order
index response returns 20 orders and must materialize no more than 100 records;
additional work belongs behind pagination.
