interface:
  display_name: "Slop Loop"
  short_description: "Review and repair a PR until it is clean"
  default_prompt: "Use $slop-loop on PR #132 until two consecutive Slopmeter passes are clean and the pull request is ready to merge."
policy:
  allow_implicit_invocation: false
